import { PUBLIC_URL } from './app.constants';

export const HOME_PAGE = `${PUBLIC_URL}/`;
export const BOOKMARKS_PAGE = `${PUBLIC_URL}/bookmarks`;
export const MOVIE_PAGE = `${PUBLIC_URL}/movie`;
export const NAVIGATION_LIST = ['Home', 'About', 'Bookmarks'];

export default {
  HOME_PAGE,
  BOOKMARKS_PAGE,
  MOVIE_PAGE,
  NAVIGATION_LIST,
};
